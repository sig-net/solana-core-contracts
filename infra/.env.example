# Infra deployment environment

# Required: JSON array string for the Solana relayer keypair
# Example: RELAYER_PRIVATE_KEY='[12,34, ...]'
RELAYER_PRIVATE_KEY=''

# Required: Alchemy API key used by relayer
NEXT_PUBLIC_ALCHEMY_API_KEY=

# Required: Solana RPC endpoint used by relayer
NEXT_PUBLIC_SOLANA_RPC_URL=

# Optional: Sepolia RPC (if not set, Alchemy URL fallback is used)
NEXT_PUBLIC_SEPOLIA_RPC_URL=

# AWS settings
AWS_REGION=us-east-1
# Optional: set if you use a named profile via `aws configure --profile <name>`
# AWS_PROFILE=

# SST stage name (eg: dev, prod)
SST_STAGE=prod
